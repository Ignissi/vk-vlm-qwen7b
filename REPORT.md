# Отчёт по проекту

## Оценка и LoRA-адаптация визуально-языковой модели Qwen2.5-VL-7B на русскоязычных данных DeepVK

**Исполнитель:** Михеева Анжелика Игоревна, группа: ИУ6-45Б
**Организация:** МГТУ им. Н. Э. Баумана
**Год:** 2026

---

## 1. Описание проекта

В рамках проекта разработан воспроизводимый экспериментальный pipeline для оценки и параметрически эффективного дообучения визуально-языковой модели на открытых русскоязычных данных DeepVK.

Визуально-языковые модели совместно обрабатывают изображение и текстовый запрос. Их практический смысл состоит в том, что изображение становится не просто файлом, а источником информации, к которому можно обращаться с вопросами на естественном языке. Например, модель может определить, какие объекты присутствуют на изображении, какие действия выполняются и как объекты расположены относительно друг друга.

В качестве базовой модели выбрана `Qwen/Qwen2.5-VL-7B-Instruct`. Для обучения и проверки использован открытый датасет `deepvk/MMBench-ru`, содержащий изображения, вопросы на русском языке, варианты ответов и эталонные ответы.

Проект моделирует задачу бизнес-юнита машинного обучения: необходимо проверить производительность выбранной визуально-языковой модели на конкретном наборе данных и определить, влияет ли LoRA-адаптация на качество её ответов.

---

## 2. Цель проекта

Разработать воспроизводимый процесс оценки и LoRA-адаптации визуально-языковой модели `Qwen2.5-VL-7B-Instruct` на открытых данных DeepVK и определить изменение точности модели при ответах на русскоязычные вопросы по изображениям.

---

## 3. Задачи проекта

1. Изучить структуру открытого датасета `deepvk/MMBench-ru`.
2. Подготовить непересекающиеся обучающую и контрольную выборки.
3. Измерить исходную точность базовой модели `Qwen2.5-VL-7B-Instruct`.
4. Выполнить параметрически эффективное дообучение методом LoRA.
5. Повторно оценить адаптированную модель на той же контрольной выборке.
6. Сравнить результаты до и после обучения.
7. Сохранить обученный адаптер, параметры запуска, итоговые метрики и подробные предсказания модели.
8. Оценить практическую применимость разработанного pipeline для предварительного сравнения визуально-языковых моделей.

---

## 4. Практическая значимость

Полученное решение может использоваться командой NLP или мультимодального машинного обучения как основа для быстрого сравнения визуально-языковых моделей на русскоязычных данных.

Разработанный pipeline позволяет:

* измерить исходное качество выбранной модели на конкретном наборе данных;
* выполнить адаптацию модели без полного переобучения всех её параметров;
* сравнить качество до и после обучения;
* сохранить ответы на отдельных примерах для анализа ошибок;
* оценить целесообразность более масштабного обучения до затрат дополнительных вычислительных ресурсов;
* воспроизводить эксперимент с другими моделями и параметрами.

Практический смысл исследуемой способности состоит в автоматическом извлечении информации из изображений по текстовому запросу. Такая технология может применяться для анализа фотоархивов, смыслового поиска изображений, проверки визуального контента и построения мультимодальных информационных систем.

Варианты ответа `A–D` в данном проекте не являются назначением модели. Они используются как простой и объективный механизм автоматической проверки того, насколько правильно модель связала русскоязычный вопрос с содержимым изображения.

---

## 5. Использованные данные

В проекте использован открытый датасет:

`deepvk/MMBench-ru`

Каждый пример содержит:

* изображение;
* вопрос на русском языке;
* дополнительные текстовые данные;
* четыре варианта ответа;
* эталонный ответ.

Датасет предоставляет split `dev`. Для учебного эксперимента данные были перемешаны с фиксированным `seed=42` и разделены на две непересекающиеся части:

* **512 примеров** — обучающая выборка;
* **128 примеров** — внутренняя контрольная выборка.

Контрольные примеры не использовались при обучении LoRA-адаптера.

Полученная accuracy является внутренней экспериментальной метрикой. Она не является официальным результатом полного бенчмарка MMBench, поскольку использовано собственное разделение единственного доступного split.

---

## 6. Выбор модели и метода обучения

### 6.1. Базовая модель

В качестве основы выбрана модель:

`Qwen/Qwen2.5-VL-7B-Instruct`

Это визуально-языковая модель, способная совместно обрабатывать изображения и текст. Модель уже предварительно обучена, поэтому задачей проекта являлось не обучение с нуля, а адаптация к выбранным русскоязычным данным.

### 6.2. Метод LoRA

Для обучения использован метод LoRA — параметрически эффективный способ адаптации больших моделей.

Основные веса базовой модели оставались замороженными. Обучались дополнительные низкоранговые матрицы в attention-проекциях:

* `q_proj`;
* `k_proj`;
* `v_proj`;
* `o_proj`.

Количество обучаемых параметров:

* обучаемые параметры: **5 046 272**;
* общее количество параметров: **8 297 212 928**;
* доля обучаемых параметров: **0,0608%**.

Таким образом, был получен отдельный LoRA-адаптер, который подключается к исходной модели и не изменяет её базовые веса.

---

## 7. Условия и параметры эксперимента

Эксперимент выполнен локально на следующем оборудовании:

* операционная система: Ubuntu 24.04;
* GPU: NVIDIA GeForce RTX 5090;
* объём видеопамяти: 32 GB;
* формат вычислений: BF16.

Основные параметры обучения:

* размер обучающей выборки: 512;
* размер контрольной выборки: 128;
* количество эпох: 2;
* learning rate: `5e-5`;
* batch size: 1;
* gradient accumulation: 4;
* метод адаптации: LoRA;
* фиксированное начальное состояние генератора: `seed=42`.

Для оценки использовалась метрика accuracy:

`accuracy = количество правильных ответов / количество контрольных примеров`

Перед обучением проводилась оценка исходной модели. После завершения обучения к той же базовой модели подключался сохранённый LoRA-адаптер и выполнялась повторная оценка на тех же 128 контрольных примерах.

---

## 8. Реализация

Эксперимент реализован на Python с использованием библиотек:

* PyTorch;
* Transformers;
* Datasets;
* PEFT;
* Accelerate.

Основной сценарий `train_and_evaluate.py` выполняет следующую последовательность:

1. Загружает датасет `deepvk/MMBench-ru`.
2. Формирует обучающую и контрольную выборки.
3. Загружает процессор и базовую модель `Qwen2.5-VL-7B-Instruct`.
4. Оценивает accuracy исходной модели.
5. Подключает LoRA-конфигурацию.
6. Обучает адаптер в течение двух эпох.
7. Сохраняет обученные LoRA-веса.
8. Повторно оценивает адаптированную модель.
9. Сохраняет метрики, конфигурацию и подробные предсказания.

В результате работы создаются:

* `adapter/adapter_model.safetensors` — обученные LoRA-веса;
* `adapter/adapter_config.json` — конфигурация адаптера;
* `results/metrics.json` — итоговые метрики;
* `results/predictions.csv` — ответы базовой и адаптированной моделей;
* `results/run_config.json` — параметры эксперимента;
* `REPORT.md` — описание проекта и результатов.

---

## 9. Полученные результаты

Время обучения составило приблизительно **206,6 секунды**.

Итоговый train loss:

`0.1233`

Результаты оценки:

| Модель                          | Правильных ответов | Всего примеров | Accuracy |
| ------------------------------- | -----------------: | -------------: | -------: |
| Исходная Qwen2.5-VL-7B-Instruct |                101 |            128 |   0.7891 |
| Модель после LoRA-адаптации     |                102 |            128 |   0.7969 |

Абсолютное изменение accuracy:

`0.7969 - 0.7891 = +0.0078`

Это соответствует увеличению на **0,78 процентного пункта** и одному дополнительному правильному ответу на контрольной выборке.

Полученный прирост является небольшим. Размер контрольной выборки не позволяет считать его статистически устойчивым доказательством общего повышения качества. Однако эксперимент показывает, что LoRA-адаптация на 512 русскоязычных мультимодальных примерах:

* сохранила качество сильной базовой модели;
* не потребовала полного переобучения 7B-модели;
* дала положительное изменение внутренней контрольной метрики;
* создала пригодный для дальнейшего использования обученный адаптер.

---

## 10. Анализ результата

Высокая исходная accuracy показывает, что `Qwen2.5-VL-7B-Instruct` уже обладает сильной способностью отвечать на вопросы по изображениям.

Небольшой прирост после LoRA объясняется следующими факторами:

1. Базовая модель уже имеет высокое исходное качество.
2. 512 обучающих примеров — небольшой объём относительно масштаба 7B-модели.
3. Контрольная выборка содержит только 128 примеров.
4. Выполнен один основной эксперимент без систематического подбора гиперпараметров.
5. Обучалась только небольшая часть параметров модели.

Результат следует интерпретировать не как значительное улучшение бенчмарка, а как подтверждение работоспособности всей цепочки адаптации и оценки.

Для более сильного исследования можно было бы:

* увеличить объём обучающих данных;
* использовать отдельные validation- и test-выборки;
* сравнить несколько визуально-языковых моделей;
* выполнить систематический подбор learning rate, LoRA rank и числа эпох;
* применить отдельный instruction-training датасет для обучения, оставив MMBench-ru только для итоговой оценки;
* провести анализ ошибок по категориям визуальных вопросов.

---

## 11. Ограничения проекта

Эксперимент имеет следующие ограничения:

* использована внутренняя часть split `dev`, а не официальный закрытый test;
* выполнено одно основное разделение данных;
* контрольная выборка ограничена 128 примерами;
* исследована одна базовая модель;
* не проводилась оценка статистической значимости;
* небольшой положительный прирост соответствует одному дополнительному правильному ответу;
* результат не следует представлять как официальный рекорд MMBench-ru.

Эти ограничения не отменяют выполненное обучение, но определяют границы возможных выводов.

---

## 12. Вывод

В ходе проекта была реализована полная цепочка оценки и параметрически эффективного обучения визуально-языковой модели на открытых данных DeepVK.

Была измерена исходная производительность `Qwen2.5-VL-7B-Instruct`, выполнено LoRA-дообучение, сохранён обученный адаптер и проведено повторное тестирование на непересекающейся контрольной выборке.

Accuracy изменилась с **0.7891** до **0.7969**. Абсолютный прирост составил **+0.0078**, или **+0,78 процентного пункта**.

Разработанное решение может быть полезно команде NLP или мультимодального машинного обучения для предварительной оценки визуально-языковых моделей на конкретном русскоязычном наборе данных, сравнения конфигураций обучения и принятия решения о целесообразности более масштабных экспериментов.

Цель проекта достигнута:

* модель адаптирована на открытых данных VK;
* качество измерено до и после обучения;
* получен и сохранён обученный LoRA-адаптер;
* сохранены код, конфигурация, метрики и подробные предсказания;
* эксперимент оформлен в воспроизводимом виде.
